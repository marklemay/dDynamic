
data Nat : * {
| Z : Nat
| S : Nat -> Nat
};

data Id : (A : *) -> A -> A -> * {
| refl  : (A : *) -> (a : A) -> Id A a a
};

badEq : Id (Nat -> Nat) (\x => 0) (\x => 1);
badEq = refl (Nat -> Nat) (\x => 0) ;
-- TODO no warning is happening here? 


--badEq2 : Id (Nat -> Nat) (\x => 0) (\x => 1);
--badEq2 = refl (Nat -> Nat) (\x => x) ;

--badEq01 : Id Nat 0 1 ;
--badEq01 = refl Nat 0 ;